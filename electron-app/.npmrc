# Electron-builder needs a flat node_modules layout to bundle correctly.
# Without this, pnpm's symlinked store breaks the file globs that
# electron-builder uses for production packaging.
node-linker=hoisted

# Allow Electron's prebuild scripts to run.
enable-pre-post-scripts=true

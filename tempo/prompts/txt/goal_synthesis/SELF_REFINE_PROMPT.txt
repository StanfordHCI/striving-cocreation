You are reviewing and refining a set of synthesized LIFE GOALS for {user_name}.

Life goals are personal strivings (Emmons, 1986) — enduring, recurring pursuits that organize multiple activities and persist beyond any single project. They sit ABOVE activities/personal projects in the goal hierarchy.

Each life goal must have three defining properties:
1. **Dispositional recurrence** — ongoing, not a one-time objective (the completability test: can it be checked off?)
2. **Active behavioral manifestation** — shows up in what {user_name} does, not just what they wish
3. **Abstraction above actions** — organizes multiple concrete activities under a common pursuit

Below is the previous synthesis output. Critique it for:

## Standard quality checks
1. **Redundancy**: Are any goals too similar and should be merged?
2. **Overly broad**: Is any goal so broad it's meaningless? (e.g., "is trying to be productive")
3. **Restating an activity**: Is any goal just a project with an endpoint? Apply the completability test. Common failure: "is finishing their dissertation" (project) instead of "is pursuing recognition as a credible independent researcher" (striving).
4. **Too surface-level**: Is any goal just a task category? Push past "manages email" toward the underlying pursuit.
5. **Too few goals**: If the synthesis produced fewer than 8 goals, consider whether you have: (a) lumped distinct strivings into one overly broad goal that should be split, (b) missed entire life domains (e.g., only work goals, no personal/social/health strivings), (c) overlooked avoidance or obligation patterns that constitute separate pursuits. But do NOT force goals that aren't supported by behavioral evidence — 5 well-grounded goals are better than 10 thin ones.
6. **Missing coverage**: Are any activities unassigned or poorly assigned?
7. **Evidence quality**: Does the evidence actually support each goal?
8. **Language clarity**: Is any goal written in academic jargon? Rewrite to sound like something a thoughtful friend would say.

## Phrasing checks
9. **Phrasing variety**: Do multiple goals use the exact same construction (e.g., all start with "is trying to...")? Vary naturally: "is pursuing," "is invested in," "is working to," "is drawn to," "is building," "is growing into," "is dedicated to," "is actively avoiding," etc.
10. **Wish vs. pursuit**: Does any goal read as a desire rather than an active pursuit? "Wants to," "hopes to," "wishes to" describe aspirations, not strivings. If there's behavioral evidence, rephrase as what {user_name} is actively doing.
11. **Behavioral grounding**: Since these are inferred from observed behavior (not self-report), does each goal describe something the system has evidence of {user_name} *doing*, not just intending?

## Theory-grounded checks
12. **Conflict completeness** (Emmons): Are all key tensions between goals captured in goal_relations?
13. **Avoidance & ambivalence gap** (Markus & Nurius): Are ALL goals pure approach? Check:
   (a) If no avoidance goals: look for procrastination, consistent avoidance, or defensive activities.
   (b) If no `mixed` goals: look for goals where {user_name} is attracted to an outcome but also fears aspects of it. For `mixed` goals, BOTH `identity_link` and `feared_self` must be populated.
14. **Ideal/ought balance** (Higgins): Is everything aspirational? Look for obligation-driven patterns: deadlines, external expectations, "should" behaviors.
15. **Autonomy signal check** (SDT): Are all goals autonomous? Look for grudging patterns: reactive behaviors, externally-triggered actions, low-engagement tasks.
16. **Level appropriateness** (Emmons): Is each goal at the right abstraction level? Not so abstract it's a need label ("wants competence"), not so concrete it's a project ("finishing the dissertation"). Apply the completability test.
17. **Synthesis quality**: Does each `synthesis_evidence` convincingly explain WHY diverse activities belong under one striving? If tautological ("these are all work activities"), it's a domain category, not a striving.
18. **Need tag accuracy**: Did need tags drive the goal text? If it reads like a need description ("is seeking competence and mastery"), rewrite as what {user_name} is actually doing.
19. **Metadata consistency**: Are people, domains, engagement/initiation signatures, and temporal_context consistent with constituent activities?
20. **Constraint compliance**: Are [locked] goals preserved? Are [user-edited] labels kept verbatim? Are user annotations reflected? NEVER merge or remove a [locked] goal.
21. **Need landscape** (if saturation data provided): Produce a `need_landscape` section identifying: (a) congruence — behavior matches inferred needs, (b) incongruence — needs claimed but starved in behavior, (c) leverage_points — activities serving multiple needs across goals (multifinality).

Previous synthesis:
{previous_output}

Activities for reference:
{activities}

{user_goals_section}

{need_saturation_context}
{user_context}
########################################
# Confidence scoring
########################################
After refining, recalibrate the confidence score for each goal:

- **Evidence breadth**: Is the goal grounded in multiple activities across different domains, or based on a single activity?
- **Behavioral manifestation**: Is there direct evidence of {user_name} actively pursuing this striving, or is it an inference from surface behavior?
- **Distinctness**: Is this goal clearly distinct from other goals, not a redundant restatement?

Score: **1 (speculative, thin evidence)** to **10 (strong multi-activity convergence with clear behavioral pattern)**.

Be conservative. Just because {user_name} uses certain apps does not mean they are actively pursuing the striving you infer. Assign high confidence (8-10) only when multiple activities show a clear, sustained behavioral pattern that directly supports the goal.

Edge cases:
- **Contradictions**: If two activities suggest conflicting goals, keep both and add a "conflict" relation rather than forcing them into one goal.
- **Granularity mismatch**: If a goal maps to only one activity, it may be restating the activity — push to the underlying striving or lower confidence.
- **Weak evidence**: Keep low-confidence goals rather than dropping them, but mark confidence accordingly.
- **Confidence recalibration**: After merging or splitting goals, recalibrate confidence based on the combined evidence — the merged/split result may be stronger or weaker than the original.

Produce a REVISED set of goals in the same JSON format. Keep goals that are good,
merge/split/revise goals that need it, and ensure every activity is covered.

CRITICAL: Preserve `activity_ids` for every goal. If you merge goals, union their activity_ids. If you split, partition them. Every activity ID must appear in at least one goal.
Preserve `people`, `domains`, `engagement_signature`, `initiation_signature`, and `temporal_context`. If merging, union people/domains lists and revise summaries for the merged set.

OUTPUT STRICT JSON (same schema as before, no prose, no markdown):
{{
  "goals": [
    {{
      "text": "{user_name} is ...",
      "needs": [...],
      "orientation": "approach|avoidance|mixed",
      "aspiration_vs_obligation": "ideal|ought|mixed",
      "autonomy": "autonomous|controlled|mixed",
      "identity_link": "... or null",
      "feared_self": "... or null",
      "activity_ids": [REQUIRED],
      "reasoning": "...",
      "synthesis_evidence": "What cross-activity pattern reveals this as one underlying striving?",
      "confidence": 8,
      "matches_goal_id": null,
      "evidence_summary": "...",
      "people": ["..."],
      "domains": ["work", "personal"],
      "engagement_signature": "...",
      "initiation_signature": "...",
      "temporal_context": "..."
    }}
  ],
  "goal_relations": [...],
  "dropped_goals": [
    {{
      "goal_id": 42,
      "goal_text": "original text",
      "reason": "merged|evidence_changed|redundant|absorbed",
      "explanation": "why this goal was dropped"
    }}
  ],
  "need_landscape": {{
    "congruence_insights": [
      {{
        "need": "competence",
        "type": "congruent",
        "insight": "...",
        "activity_ids": [4, 7]
      }}
    ],
    "incongruence_insights": [
      {{
        "need": "relatedness",
        "type": "starved",
        "insight": "...",
        "activity_ids": []
      }}
    ],
    "leverage_points": [
      {{
        "activity_id": 12,
        "activity_text": "...",
        "activity_ids": [4, 7]
      }}
    ]
  }}
}}